spring:
  application:
    name: msvc-eureka
  config:
    # this is the path where the configuration file is located and the port where the configuration server is running
    import: optional:configserver:http://localhost:8888